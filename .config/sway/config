# Default config for sway
#
# Copy this to ~/.config/sway/config and edit it to your liking.
#
# Read `man 5 sway` for a complete reference.

### Variables
#
# Logo key. Use Mod1 for Alt.
set $mod Mod4
# Home row direction keys, like vim
set $left h
set $down j
set $up k
set $right l
font pango:JetBrainsMono Nerd Font 10
set $term kitty
# Common apps and scripts (ported from Hyprland)
set $explorer nemo
# set $explorer dolphin
set $notes obsidian
set $browser firefox
set $editor nvim
set $notifycmd notify-send
set $scripts $HOME/scripts
set $launcher $scripts/run_launcher.sh
set $power $scripts/powermenu.sh
set $swaync swaync-client
# Define workspaces for Waybar to show
workspace 1
workspace 2
workspace 3
workspace 4
workspace 5

# Your preferred application launcher
# Note: pass the final command to swaymsg so that the resulting window can be opened
# on the original workspace that the command was run on.
set $menu $launcher

include /etc/sway/config-vars.d/*



### Output configuration
#
# Default wallpaper (more resolutions are available in /usr/share/backgrounds/sway/)
# output * bg /usr/share/backgrounds/sway/Sway_Wallpaper_Blue_1920x1080.png fill

# Environment (ported from Hyprland envs) for Wayland apps on Ubuntu
# setenv XCURSOR_SIZE 24
# setenv XCURSOR_THEME Bibata-Modern-Classic
# setenv QT_QPA_PLATFORM wayland
# setenv QT_WAYLAND_DISABLE_WINDOWDECORATION 1
# setenv QT_AUTO_SCREEN_SCALE_FACTOR 1
# setenv GDK_BACKEND wayland
# setenv MOZ_ENABLE_WAYLAND 1
# setenv XDG_SESSION_TYPE wayland
# setenv XDG_CURRENT_DESKTOP sway
# setenv XDG_SESSION_DESKTOP sway
#
# Example configuration:
#
#   output HDMI-A-1 resolution 1920x1080 position 1920,0
#
# You can get the names of your outputs by running: swaymsg -t get_outputs

### Idle configuration
#
# swayidle replacing hypridle
# exec_always --no-startup-id swayidle -w \
#          timeout 420 '$HOME/scripts/lock_with_matrix.sh' \
#          timeout 600 'swaymsg "output * power off"' resume 'swaymsg "output * power on"' \
#          before-sleep '$HOME/scripts/lock_with_matrix.sh'

# exec_always --no-startup-id swayidle -w timeout 300 'swaylock -f -c 000000'

# If you don't have the lock script, install swaylock and swap the command above with: swaylock -f -c 000000

### Input configuration
#
# Example configuration:
#
#   input "2:14:SynPS/2_Synaptics_TouchPad" {
#       dwt enabled
#       tap enabled
#       natural_scroll enabled
#       middle_emulation enabled
#   }
#
# You can get the names of your inputs by running: swaymsg -t get_inputs
# Read `man 5 sway-input` for more information about this section.

### Key bindings
#
# Basics:
#
#    # Start a terminal
    bindsym $mod+Return exec $term
    bindsym $mod+t exec warp-terminal 
    bindsym $mod+x exec kitty

    # Kill focused window (ported from Hyprland $mod+q)
    bindsym $mod+q kill
    # Power menu on Shift+Q (replaces default kill binding)
    bindsym $mod+Shift+q exec $power

    # Start your launcher (uses $launcher)
    bindsym $mod+d exec $menu
    # bindsym $mod+d exec sh -c 'grep -l "^Exec=" /usr/share/applications/*.desktop ~/.local/share/applications/*.desktop 2>/dev/null | while read desktop; do name="$(grep "^Name=" "$desktop" | head -n1 | cut -d"=" -f2-)"; exec_cmd="$(grep "^Exec=" "$desktop" | head -n1 | cut -d"=" -f2- | sed "s/%[fFuUcCDi]*//g")"; printf "%s\t%s\n" "$name" "$exec_cmd"; done | fuzzel --dmenu --prompt "Launch" | cut -f2- | sh'

    # Notifications toggle
    bindsym $mod+c exec "$swaync -t -sw"

    # File manager, browser, editor
    bindsym $mod+n exec $explorer
    bindsym $mod+b exec $google-chrome
    # bindsym $mod+n exec $editor
    bindsym $mod+z exec zen-browser

    # Toggle floating / fullscreen
    bindsym $mod+Shift+f floating toggle
    bindsym $mod+f fullscreen
    bindsym $mod+Ctrl+f fullscreen toggle global

    # Custom scripts
    bindsym $mod+u exec "$scripts/bluelight.sh --toggle"
    # bindsym $mod+Ctrl+w exec waypaper
    bindsym $mod+Ctrl+w exec bash -lc waypaper
    bindsym $mod+r exec "$scripts/radio.sh"

    # Screenshots (grim/slurp replacement for hyprshot)
    set $shots $HOME/Pictures/Screenshots
    bindsym $mod+Print exec bash -lc 'mkdir -p "$shots"; grim "$shots/$(date +"%F_%H-%M-%S").png"'
    bindsym Print exec bash -lc 'mkdir -p "$shots"; grim -g "$(slurp)" "$shots/$(date +"%F_%H-%M-%S")_sel.png"'

    # Color picker (replacement for hyprpicker)
    bindsym $mod+Shift+p exec wl-color-picker -f hex -a

    # Lock screen
    # bindsym $mod+l exec "$scripts/lock_with_matrix.sh"
    bindsym $mod+l exec "swaylock -f -c 000000"

    # Drag floating windows by holding down $mod and left mouse button.
    # Resize them with right mouse button + $mod.
    # Despite the name, also works for non-floating windows.
    # Change normal to inverse to use left mouse button for resizing and right
    # mouse button for dragging.
    floating_modifier $mod normal

    # Reload the configuration file
    bindsym $mod+Shift+c reload
    bindsym $mod+Shift+r reload
    # Restart Waybar
    bindsym $mod+Shift+w exec sh -lc 'pkill waybar && waybar &'

    # Exit sway (logs you out of your Wayland session)
    bindsym $mod+Shift+e exec swaynag -t warning -m 'You pressed the exit shortcut. Do you really want to exit sway? This will end your Wayland session.' -B 'Yes, exit sway' 'swaymsg exit'
#
# Moving around:
#
    # Move your focus around
    bindsym $mod+$left focus left
    bindsym $mod+$down focus down
    bindsym $mod+$up focus up
    # bindsym $mod+$right focus right
    # Cycle focus (replacement for hypr cyclenext)
    bindsym $mod+Tab focus next
    bindsym Mod1+Tab exec wofi --show window
    # Or use $mod+[up|down|left|right]
    bindsym $mod+Left focus left
    bindsym $mod+Down focus down
    bindsym $mod+Up focus up
    bindsym $mod+Right focus right

    # Move the focused window with the same, but add Shift
    bindsym $mod+Shift+$left move left
    bindsym $mod+Shift+$down move down
    bindsym $mod+Shift+$up move up
    bindsym $mod+Shift+$right move right
    # Ditto, with arrow keys
    bindsym $mod+Shift+Left move left
    bindsym $mod+Shift+Down move down
    bindsym $mod+Shift+Up move up
    bindsym $mod+Shift+Right move right
#
# Workspaces:
#
    # Switch to workspace
    bindsym $mod+1 workspace number 1
    bindsym $mod+2 workspace number 2
    bindsym $mod+3 workspace number 3
    bindsym $mod+4 workspace number 4
    bindsym $mod+5 workspace number 5
    bindsym $mod+6 workspace number 6
    bindsym $mod+7 workspace number 7
    bindsym $mod+8 workspace number 8
    bindsym $mod+9 workspace number 9
    bindsym $mod+0 workspace number 10
    # Move focused container to workspace
    bindsym $mod+Shift+1 move container to workspace number 1
    bindsym $mod+Shift+2 move container to workspace number 2
    bindsym $mod+Shift+3 move container to workspace number 3
    bindsym $mod+Shift+4 move container to workspace number 4
    bindsym $mod+Shift+5 move container to workspace number 5
    bindsym $mod+Shift+6 move container to workspace number 6
    bindsym $mod+Shift+7 move container to workspace number 7
    bindsym $mod+Shift+8 move container to workspace number 8
    bindsym $mod+Shift+9 move container to workspace number 9
    bindsym $mod+Shift+0 move container to workspace number 10

    # Move focused container to relative workspace (Hyprland r+1 / r-1)
    bindsym $mod+Ctrl+Alt+Right move container to workspace next
    bindsym $mod+Ctrl+Alt+Left move container to workspace prev

    # Note: workspaces can have any name you want, not just numbers.
    # We just use 1-10 as the default.
#
# Layout stuff:
#
    # You can "split" the current object of your focus with
    # $mod+b or $mod+v, for horizontal and vertical splits
    # respectively.
    # bindsym $mod+b splith
    # bindsym $mod+v splitv

    # Switch the current container between different layout styles
    bindsym $mod+i layout stacking
    bindsym $mod+w layout tabbed
    bindsym $mod+e layout toggle split

    # Make the current focus fullscreen
    # bindsym $mod+f fullscreen

    # Toggle the current focus between tiling and floating mode
    bindsym $mod+Shift+space floating toggle

    # Swap focus between the tiling area and the floating area
    bindsym $mod+space focus mode_toggle

    # Move focus to the parent container
    bindsym $mod+a focus parent
#
# Scratchpad:
#
    # Sway has a "scratchpad", which is a bag of holding for windows.
    # You can send windows there and get them back later.

    # Move the currently focused window to the scratchpad
    # bindsym $mod+Shift+minus move scratchpad

    # Show the next scratchpad window or hide the focused scratchpad window.
    # If there are multiple scratchpad windows, this command cycles through them.
    # bindsym $mod+minus scratchpad show

    # Hyprland "special" workspace rough equivalent:
    bindsym $mod+s scratchpad show
    bindsym $mod+Shift+s move scratchpad
#
# Resizing containers:
# Additional direct resize binds (ported from Hyprland $mod+Ctrl+h/j/k/l)
    bindsym $mod+Ctrl+h resize shrink width 25px
    bindsym $mod+Ctrl+l resize grow width 25px
    bindsym $mod+Ctrl+k resize shrink height 25px
    bindsym $mod+Ctrl+j resize grow height 25px

mode "resize" {
    # left will shrink the containers width
    # right will grow the containers width
    # up will shrink the containers height
    # down will grow the containers height
    bindsym $left resize shrink width 10px
    bindsym $down resize grow height 10px
    bindsym $up resize shrink height 10px
    bindsym $right resize grow width 10px

    # Ditto, with arrow keys
    bindsym Left resize shrink width 10px
    bindsym Down resize grow height 10px
    bindsym Up resize shrink height 10px
    bindsym Right resize grow width 10px

    # Return to default mode
    bindsym Return mode "default"
    bindsym Escape mode "default"
}
# bindsym $mod+r mode "resize"

#
# Status Bar:
#
# Read `man 5 sway-bar` for more information about this section.

# Autostart (ported from Hyprland execs)
exec --no-startup-id dbus-update-activation-environment --systemd WAYLAND_DISPLAY SWAYSOCK XDG_CURRENT_DESKTOP
exec --no-startup-id /usr/lib/policykit-1-gnome/polkit-gnome-authentication-agent-1
# exec --no-startup-id waybar
# exec --no-startup-id quickshell -c $HOME/.config/quickshell/jackbar/shell.qml
exec --no-startup-id quickshell -p "$HOME/dotfiles/.config/quickshell/jackbar"
exec --no-startup-id swaync
# exec_always --no-startup-id dunst
# Clipboard history
exec --no-startup-id sh -c 'wl-paste --type text --watch cliphist store &'
exec --no-startup-id sh -c 'wl-paste --type image --watch cliphist store &'
exec --no-startup-id copyq
# Ensure workspaces 1..5 exist at startup for Waybar
exec --no-startup-id swaymsg 'workspace number 1; workspace number 2; workspace number 3; workspace number 4; workspace number 5; workspace number 1'
# KDE Connect indicator
# exec_always --no-startup-id sh -c 'sleep 5 && nohup kdeconnect-indicator >/dev/null 2>&1 &'
# Wallpaper
exec --no-startup-id $HOME/scripts/restore_wallpaper.sh
# User scripts
# exec_always --no-startup-id sh -c 'sleep 15 && "$HOME"/scripts/backup.sh'
# for remote connect via rustdesk
exec xdg-desktop-portal-wlr

# Messengers: place on workspace 3
for_window [class="TelegramDesktop"] move to workspace 3
for_window [app_id="telegram-desktop"] move to workspace 3
for_window [app_id="org.telegram.desktop"] move to workspace 3
for_window [class="Viber"] move to workspace 3
for_window [app_id="com.viber.Viber"] move to workspace 3
for_window [app_id="Viber"] move to workspace 3

# mpv: fixed size window (60% width, 50% height)
for_window [app_id="mpv"] resize set width 60 ppt height 50 ppt
for_window [class="mpv"] resize set width 60 ppt height 50 ppt

# Multimedia keys
bindsym XF86AudioRaiseVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%+
bindsym XF86AudioLowerVolume exec wpctl set-volume @DEFAULT_AUDIO_SINK@ 5%-
bindsym XF86AudioMute exec wpctl set-mute @DEFAULT_AUDIO_SINK@ toggle
bindsym XF86AudioMicMute exec wpctl set-mute @DEFAULT_AUDIO_SOURCE@ toggle
bindsym XF86MonBrightnessUp exec brightnessctl s 10%+
bindsym XF86MonBrightnessDown exec brightnessctl s 10%-
# Media control
bindsym XF86AudioPlay exec playerctl --player=playerctld play-pause
bindsym XF86AudioPause exec playerctl --player=playerctld play-pause
bindsym XF86AudioNext exec playerctl --player=playerctld next
bindsym XF86AudioPrev exec playerctl --player=playerctld previous

# Float everything by default (Wayland + Xwayland)
for_window [app_id=".*"] floating enable
for_window [class=".*"] floating enable

# Override floating for specific apps that should be tiled
# Warp terminal should always be tiled
for_window [app_id="dev.warp.Warp"] floating disable
for_window [class="dev.warp.Warp"] floating disable
for_window [title=".*Warp.*"] floating disable

# Slurp should work as layer-shell overlay
for_window [app_id="slurp"] border none, floating enable, sticky enable

### Opacity apps
for_window [app_id="nemo"] opacity 0.9
for_window [app_id="zen"] opacity 0.95

# Цвета: border, bg, text, indicator, child_border
# client.focused          #4c7899 #285577 #ffffff #2e9ef4 #285577
# client.focused          #4c7899 #551133AA #ffffff #2e9ef4 #285577
client.focused          #4c7899 #55116677 #ffffff #2e9ef4 #285577


# Workspaces list circle
bindsym --whole-window $mod+button4 workspace prev_on_output
bindsym --whole-window $mod+button5 workspace next_on_output

# OBS quick bind
bindsym $mod+F10 exec obs

# Language/layouts (ported from Hyprland input settings)
input * {
    xkb_layout us,ru
    xkb_options grp:alt_shift_toggle
}

include /etc/sway/config.d/*
