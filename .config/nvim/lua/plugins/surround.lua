-- easily delete, change and add surroundings in pairs
-- cs"', cst", ds", ysiw], cs]{, yss), ysiw<em>, VS[

return {
    "tpope/vim-surround",
    event = "VeryLazy",
    -- keys = { "ys", "ds", "cs" },
}
